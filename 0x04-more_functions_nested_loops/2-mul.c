#include "main.h"

/**
* mul-> multiplies two numbers and give the output
*@a:imultiplicity
* @b: is the numbers to be multiplied
* Return: return the output mul
*/

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
