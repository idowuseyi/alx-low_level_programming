#include "main.h"

/**
* _isdigit-> check if a value is a digit between 9 and 0
* @c: the integer to be checked
* Return: return 1 if true and 0 otherwise
*/

int _isdigit(int c)
{
	int c;

	return (c >= 0 && c <= 9);
}
